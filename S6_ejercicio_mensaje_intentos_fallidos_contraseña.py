
# Define a variable to store the username
user = "pablo_1990"

# Define a variable to store the error message
text = ", ¡tu contraseña es incorrecta! Te quedan 2 intentos."

# Concatenate the username and the error message
# Then, print the result
print(user + text)
#
#In this code, we define two variables: `user` and `text`. The `user` variable stores the username, while the `text` variable stores the error message.
#
#Next, we concatenate the `user` and `text` variables using the `+` operator. This operation combines the two strings into one.
#
#Finally, we use the `print()` function to display the concatenated string. The output will be: `pablo_1990, ¡tu contraseña